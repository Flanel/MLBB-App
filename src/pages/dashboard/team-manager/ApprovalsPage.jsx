// Team Manager version of ApprovalsPage — identical logic, filtered to own team
// Re-exports the same component (team_id filter is enforced by Supabase RLS)
export { default } from '@/pages/dashboard/super-admin/ApprovalsPage'
